<% if obj.errors.any? %>
	<div class = "bg-danger">
		<div style="padding:10px; font-size:14px; text-align: left;">
			Oops! We found a few errors:
			<ul>
				<%obj.errors.full_messages.each do |n| %>
					<li>
						<%= n %>
					</li>
				<% end %>
			</ul>
		</div>
	</div>
<% end %>